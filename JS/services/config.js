/*
 * Configuración pública del cliente.
 * Para conectar Supabase, completa las dos variables desde el panel del proyecto.
 * La anon key es una clave pública del navegador; NUNCA pongas aquí la service_role key.
 */
window.TuCanchaConfig = Object.freeze({
    provider: "demo", // "demo" o "supabase"
    supabaseUrl: "",
    supabaseAnonKey: ""
});
